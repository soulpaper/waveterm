---
sidebar_position: 20
id: "jira-widget"
title: "Jira 위젯 설정 가이드"
---

# Jira 위젯 설정 가이드

Waveterm의 Jira Tasks 위젯은 Atlassian Jira Cloud에서 본인에게 할당된 이슈를
터미널 작업공간 안에서 바로 확인·분석할 수 있게 해줍니다. 이 문서는 처음
설치한 팀원이 5분 안에 위젯에 자기 이슈가 뜨게 만드는 길을 안내합니다.

## Jira 위젯 소개

- 본인에게 assign된 이슈 목록을 카드로 표시 (요약, 상태, 업데이트 시각, 댓글 수)
- 카드 확장 시 description·attachments·comments 조회
- `☁️` 버튼 한 번으로 Waveterm 백엔드(Go)가 Jira REST API를 호출해 캐시를 갱신
- 분석 버튼은 선택한 CLI(`claude`, `gemini` 등)에 프롬프트를 전달
- AI는 **선택**입니다. Jira 데이터 조회 자체는 AI 없이 동작합니다.

## 사전 준비

- Atlassian 계정 (KakaoVX 내부 사이트: `https://<your-site>.atlassian.net`)
- Jira Cloud 접근 권한 (JQL `assignee = currentUser()`로 조회 가능해야 함)
- Waveterm v0.14.4 이상 (`wsh jira refresh` 명령이 포함된 빌드)

## PAT(API token) 발급

1. 브라우저에서 [https://id.atlassian.com/manage-profile/security/api-tokens](https://id.atlassian.com/manage-profile/security/api-tokens) 에 접속
2. **Create API token** 클릭 → Label에 `waveterm-jira` 같이 알아볼 수 있는 이름 입력
3. 생성된 토큰을 **즉시** 복사 (다시 표시되지 않습니다)
4. 이 토큰이 아래 `jira.json`의 `apiToken` 값이 됩니다

> 팁: PAT은 비밀번호 수준의 권한을 가집니다. `jira.json`은 권한 `0600`으로 저장하세요 (Windows는 사용자 홈 폴더 ACL로 보호됩니다).

## `jira.json` 작성

`~/.config/waveterm/jira.json` 경로에 아래 템플릿을 붙여넣고 값만 바꾸세요.

```json
{
  "baseUrl": "https://<YOUR_SITE>.atlassian.net",
  "cloudId": "<YOUR_CLOUD_ID>",
  "email": "<your@email.com>",
  "apiToken": "<PASTE_PAT_HERE>",
  "jql": "assignee = currentUser() ORDER BY updated DESC",
  "pageSize": 50
}
```

필드 설명:

| 필드 | 설명 | 예시 |
|---|---|---|
| `baseUrl` | Atlassian 사이트 URL. 끝 슬래시 없이. | `https://kakaovx.atlassian.net` |
| `cloudId` | 사이트의 고유 UUID. 아래 `## 검증` 바로 위의 `curl` 한 줄로 조회 가능. | `11111111-2222-3333-4444-555555555555` |
| `email` | Atlassian 로그인 이메일 (Basic auth의 username) | `you@kakaovx.com` |
| `apiToken` | 위에서 발급한 PAT | `ATATT3xFfGF0...` |
| `jql` | 가져올 이슈를 선택하는 JQL. 기본값은 "내가 assign된 최근 업데이트 순". | `assignee = currentUser() ORDER BY updated DESC` |
| `pageSize` | 한 번에 가져올 이슈 수 (최대 100) | `50` |

### cloudId를 어떻게 알아내나요?

Atlassian 계정으로 브라우저에 로그인된 상태에서 아래 URL을 열면 JSON이 바로 반환됩니다:

```
https://<your-site>.atlassian.net/_edge/tenant_info
```

또는 터미널에서 (브라우저 세션 쿠키가 없어도 이 엔드포인트는 공개입니다):

```bash
curl -s https://<your-site>.atlassian.net/_edge/tenant_info | jq -r .cloudId
```

> 주의: `<your-site>`을 실제 사이트 이름으로 바꿔야 합니다 (예: `kakaovx`).

## 검증

`jira.json`을 저장한 후 아무 터미널에서 다음을 실행하세요:

```bash
wsh jira refresh
```

성공하면 다음과 비슷한 출력이 나옵니다:

```
✔ Refreshed: 23 issues · 7 attachments · 41 comments · 1240ms
cache: /Users/you/.config/waveterm/jira-cache.json
```

이 시점에 Jira Tasks 위젯을 열거나 이미 열려 있다면 `☁️` 버튼을 눌러
위젯이 캐시를 다시 읽게 하세요. 이슈 목록이 뜹니다.

실패 시 위젯 카드와 `wsh jira refresh` CLI 둘 다 같은 한국어 메시지를
보여줍니다 (예: `"인증 실패 — ..."`). 메시지별 대처는 § 문제 해결 참고.

## Claude에게 맡기기

위의 PAT 발급·`jira.json` 작성·검증을 직접 하기 귀찮다면, 아래 프롬프트를
**Claude 세션 터미널**에 그대로 붙여넣으세요. Claude가 필요한 값을 물어보고
파일을 올바른 권한으로 저장해 줍니다.

```text
~/.config/waveterm/jira.json 파일을 만들어줘. Atlassian site URL, cloudId, email, PAT(api token), JQL(assignee = currentUser() ORDER BY updated DESC), pageSize(50)을 물어봐서 채워줘. 파일은 권한 0600으로 저장.
```

> 위의 프롬프트는 위젯 측 단일 진실원인 `frontend/app/view/jiratasks/jiratasks-errorstate.ts`의 `CLAUDE_SETUP_PROMPT` 상수와 **바이트 단위로 동일**해야 합니다. 위젯의 **"Claude에게 자동 설정 요청"** 버튼을 누르면 이 프롬프트가 클립보드에 복사됩니다. Claude 터미널에서 `Ctrl+V` / `⌘V` 하면 됩니다.

> 스크린샷 자리표시자: 위젯 설정 카드 / 프롬프트 복사 토스트 (추후 UAT 후 추가).

## 문제 해결

위젯이 표시하는 오류 카드와 `wsh jira refresh` 메시지는 동일한 분류를 씁니다.

| 위젯 카드 | 메시지 앞부분 | 무엇을 고쳐야 하나요 |
|---|---|---|
| **Jira 설정 필요** | `설정 파일이 없습니다` | `~/.config/waveterm/jira.json`이 없습니다. § `jira.json` 작성으로 돌아가 파일을 만드세요. |
| **인증 실패 — PAT 재발급 필요** | `인증 실패` | PAT이 만료되었거나 오타입니다. [Atlassian PAT 페이지](https://id.atlassian.com/manage-profile/security/api-tokens)에서 새 토큰을 발급받아 `apiToken` 값을 교체하세요. `email` 오타도 함께 확인하세요. |
| **네트워크 오류** | `Jira 서버에 연결할 수 없습니다` | VPN·사내 프록시·인터넷 연결을 확인하세요. 다시 시도 버튼으로 재시도합니다. |
| **(속도 제한)** | `Jira 서버가 요청을 제한했습니다` | 짧은 시간 안에 너무 많이 호출했습니다. 잠시 후 다시 시도하세요. Phase 5에서 자동 재시도가 추가됩니다. |
| **새로고침 실패** | `refresh failed:` | 위 카테고리 어디에도 속하지 않는 경우입니다. 터미널에서 `wsh jira refresh`로 직접 실행해 전체 메시지를 확인하세요. `baseUrl`의 철자(`https://` 포함 여부 등)를 먼저 의심하세요. |

### 자주 하는 실수

- `baseUrl`에 끝 슬래시(`/`)를 붙인다 → 400/404 가능
- `cloudId`에 따옴표가 빠지거나 JSON 끝에 쉼표가 남는다 → 파일 파싱 실패 → "Jira 캐시를 읽을 수 없습니다"
- `email` 대신 Atlassian 로그인 **username**을 쓴다 → 401. Basic auth는 **이메일 + PAT**입니다.
- PAT을 Bitbucket 앱 비밀번호로 착각한다 → 401. 반드시 `id.atlassian.com`에서 발급한 API token.

---

이 페이지는 Waveterm v1.0 Jira 팀 연동 마일스톤의 설정 가이드입니다. 버그나 문서 누락은
내부 저장소 이슈로 올려주세요.
