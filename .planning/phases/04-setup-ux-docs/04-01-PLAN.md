---
phase: 04-setup-ux-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/view/jiratasks/jiratasks-errorstate.ts
  - frontend/app/view/jiratasks/jiratasks-errorstate.test.ts
  - frontend/app/view/jiratasks/jiratasks.tsx
  - frontend/app/view/jiratasks/jiratasks.scss
autonomous: true
requirements:
  - JIRA-04
must_haves:
  truths:
    - "When jira.json is missing, widget renders a 'setup' card (not the legacy single-line banner) with fa-gear icon and two CTAs"
    - "When apiToken is invalid (backend returns '인증 실패' prefix), widget renders an 'auth' card with fa-key icon and an 'Atlassian PAT 페이지 열기' CTA that opens a Waveterm webview block"
    - "When the backend returns a network-class error ('Jira 서버에 연결할 수 없습니다' prefix), widget renders a 'network' card with fa-wifi icon and a '다시 시도' CTA wired to requestJiraRefresh()"
    - "Any other non-null errorAtom value renders the 'unknown' card with the raw (sanitized) message shown verbatim and fa-triangle-exclamation icon"
    - "Empty-cache case (raw is '' but no other error) renders the 'setup' card variant with copy '아직 새로고침하지 않았습니다' and a ☁️ CTA that triggers the existing refresh flow"
    - "'Claude에게 자동 설정 요청' button writes the exact locked prompt (D-UI-03) to navigator.clipboard and shows a toast/inline confirmation '클립보드에 복사됨 — Claude 터미널에 붙여넣기'"
    - "classifyErrorMessage is a pure function (string -> ErrorState) exported from jiratasks-errorstate.ts and has passing unit tests covering all 4 D-STATE-01..04 prefixes plus null/empty/unknown"
    - "The legacy single-line error banner at jiratasks.tsx:1082-1087 is removed (D-REG-01) — no duplicated intent remains"
  artifacts:
    - path: "frontend/app/view/jiratasks/jiratasks-errorstate.ts"
      provides: "classifyErrorMessage helper + ErrorState type + CLAUDE_SETUP_PROMPT constant"
      exports: ["ErrorState", "classifyErrorMessage", "CLAUDE_SETUP_PROMPT", "ATLASSIAN_PAT_URL"]
      min_lines: 40
    - path: "frontend/app/view/jiratasks/jiratasks-errorstate.test.ts"
      provides: "Vitest unit tests for classifyErrorMessage covering D-STATE-01..04"
      contains: "describe('classifyErrorMessage'"
      min_lines: 40
    - path: "frontend/app/view/jiratasks/jiratasks.tsx"
      provides: "Widget renders distinct error-state cards based on classifyErrorMessage output; legacy banner removed"
      contains: "classifyErrorMessage"
    - path: "frontend/app/view/jiratasks/jiratasks.scss"
      provides: "Styles for .jiratasks-state-card, .state-setup, .state-auth, .state-network, .state-unknown (purple accent per existing '댓글' section)"
      contains: ".jiratasks-state-card"
  key_links:
    - from: "frontend/app/view/jiratasks/jiratasks.tsx (render block)"
      to: "frontend/app/view/jiratasks/jiratasks-errorstate.ts (classifyErrorMessage)"
      via: "import { classifyErrorMessage, CLAUDE_SETUP_PROMPT, ATLASSIAN_PAT_URL } from './jiratasks-errorstate'"
      pattern: "classifyErrorMessage\\("
    - from: "widget setup CTA button"
      to: "navigator.clipboard.writeText(CLAUDE_SETUP_PROMPT)"
      via: "onClick handler"
      pattern: "navigator\\.clipboard\\.writeText"
    - from: "widget auth CTA button"
      to: "createBlock({ meta: { view: 'web', url: ATLASSIAN_PAT_URL } })"
      via: "onClick handler — reuses existing openIssueInBrowser pattern (jiratasks.tsx:658)"
      pattern: "createBlock.*view.*web"
    - from: "widget network CTA button"
      to: "model.requestJiraRefresh()"
      via: "onClick handler — reuses existing ☁️ button flow"
      pattern: "requestJiraRefresh|loadFromCache"
---

<objective>
Replace the widget's single-line error banner with a 4-state, actionable error-card system driven by a pure `classifyErrorMessage(msg)` helper. States map the Korean error-string prefixes emitted by `pkg/wshrpc/wshserver/wshserver-jira.go:mapJiraError` (contracts locked by CONTEXT D-STATE-01..D-STATE-04) to distinct visual cards.

Purpose: JIRA-04 — "User who launches the widget without a valid `jira.json` sees an actionable empty state ... Invalid token / 401 from Jira shows a distinct '인증 실패 — PAT 재발급 필요' message." The current banner (line 1082-1087) shows one gray line regardless of cause — this plan makes each failure visually distinct and each card carry a fix action.

Output:
- `jiratasks-errorstate.ts` — pure helper, testable without React
- `jiratasks-errorstate.test.ts` — vitest suite (uses existing vitest harness, package.json:27,68)
- `jiratasks.tsx` — switch render + 4 card branches + empty-cache setup variant
- `jiratasks.scss` — card styles with purple accent for CTA buttons
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-setup-ux-docs/04-CONTEXT.md

# Widget implementation target (contains legacy error banner + existing atoms)
@frontend/app/view/jiratasks/jiratasks.tsx
@frontend/app/view/jiratasks/jiratasks.scss

# Backend error-string contracts — prefixes we classify on
@pkg/wshrpc/wshserver/wshserver-jira.go

<interfaces>
<!-- Locked string contracts from pkg/wshrpc/wshserver/wshserver-jira.go (mapJiraError, lines 86-99). -->
<!-- classifyErrorMessage MUST match on these prefixes, not the full string. -->

From pkg/wshrpc/wshserver/wshserver-jira.go:mapJiraError:
```go
// D-STATE-01 — setup
"설정 파일이 없습니다. Claude에게 jira 설정 생성을 요청하세요."

// D-STATE-02 — auth
"인증 실패 — ~/.config/waveterm/jira.json의 apiToken을 확인하세요 (PAT 만료/오타 가능)"

// D-STATE-03 — network
"Jira 서버에 연결할 수 없습니다: <sanitized>"
// Note: ErrRateLimited also uses "Jira 서버가 요청을 제한했습니다..." — route to network state.

// D-STATE-04 — unknown (default)
"refresh failed: <sanitized>"
```

Existing widget load-time messages (jiratasks.tsx lines 494, 526) ALSO feed into errorAtom:
```ts
// empty cache branch (line 494) — classify as D-STATE-01 setup variant per D-UI-04
"캐시 파일이 비어있습니다. Claude에게 'jira 이슈 새로고침'을 요청하세요."
// read-failure branch (line 526) — classify as unknown
"Jira 캐시를 읽을 수 없습니다. Claude에게 'jira 이슈 새로고침'을 요청하세요."
```

Existing widget atoms and methods to reuse (from jiratasks.tsx JiraTasksModel):
```ts
errorAtom: PrimitiveAtom<string | null>          // line 230 — source of truth, unchanged
loadingAtom: PrimitiveAtom<boolean>
refreshProgressAtom: Atom<string>
requestJiraRefresh(): Promise<void>              // existing ☁️ flow (Phase 3)
loadFromCache(): Promise<void>                   // line 484
```

Existing webview-open pattern (jiratasks.tsx:651,658) — reuse for "Atlassian PAT 페이지 열기":
```ts
await createBlock({ meta: { view: "web", url: ATLASSIAN_PAT_URL } });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create classifyErrorMessage helper + Nyquist-RED vitest file</name>
  <files>frontend/app/view/jiratasks/jiratasks-errorstate.ts, frontend/app/view/jiratasks/jiratasks-errorstate.test.ts</files>
  <behavior>
    Unit-test expectations (write FIRST, must fail before implementation exists):
    - `classifyErrorMessage(null)` → `null`
    - `classifyErrorMessage("")` → `null`
    - `classifyErrorMessage("설정 파일이 없습니다. Claude에게 jira 설정 생성을 요청하세요.")` → `"setup"` (D-STATE-01)
    - `classifyErrorMessage("캐시 파일이 비어있습니다. Claude에게 'jira 이슈 새로고침'을 요청하세요.")` → `"setup"` (D-UI-04 empty-cache variant)
    - `classifyErrorMessage("인증 실패 — ~/.config/waveterm/jira.json의 apiToken을 확인하세요 (PAT 만료/오타 가능)")` → `"auth"` (D-STATE-02)
    - `classifyErrorMessage("Jira 서버에 연결할 수 없습니다: dial tcp 1.2.3.4:443 connect: connection refused")` → `"network"` (D-STATE-03)
    - `classifyErrorMessage("Jira 서버가 요청을 제한했습니다. 잠시 후 다시 시도하세요.")` → `"network"` (rate-limit also maps to network per D-STATE-03)
    - `classifyErrorMessage("refresh failed: something weird")` → `"unknown"` (D-STATE-04 default)
    - `classifyErrorMessage("Jira 캐시를 읽을 수 없습니다. Claude에게 'jira 이슈 새로고침'을 요청하세요.")` → `"unknown"` (local read-failure path)
    - `CLAUDE_SETUP_PROMPT` string constant equals the exact text locked in CONTEXT D-UI-03 (byte-identical; used by test `expect(CLAUDE_SETUP_PROMPT).toContain("jira.json 파일을 만들어줘")` AND `expect(CLAUDE_SETUP_PROMPT).toContain("권한 0600으로 저장")`)
    - `ATLASSIAN_PAT_URL` equals `"https://id.atlassian.com/manage-profile/security/api-tokens"`
  </behavior>
  <action>
    Step 1 (RED). Create `frontend/app/view/jiratasks/jiratasks-errorstate.test.ts`. Use vitest (already in devDependencies, package.json:68):
    ```ts
    import { describe, it, expect } from "vitest";
    import { classifyErrorMessage, CLAUDE_SETUP_PROMPT, ATLASSIAN_PAT_URL } from "./jiratasks-errorstate";
    ```
    Write all test cases from the `<behavior>` block. Run `npx vitest run frontend/app/view/jiratasks/jiratasks-errorstate.test.ts` — MUST fail (module not found).

    Step 2 (GREEN). Create `frontend/app/view/jiratasks/jiratasks-errorstate.ts`:
    ```ts
    // Copyright 2026, Command Line Inc.
    // SPDX-License-Identifier: Apache-2.0
    //
    // jiratasks-errorstate.ts — pure helpers for mapping backend error strings
    // to visual widget states. String prefixes are LOCKED contracts with
    // pkg/wshrpc/wshserver/wshserver-jira.go:mapJiraError (CONTEXT D-STATE-01..04).
    // Do NOT translate or reword these prefixes without also updating CONTEXT
    // and the Go handler.

    export type ErrorState = "setup" | "auth" | "network" | "unknown";

    export const ATLASSIAN_PAT_URL = "https://id.atlassian.com/manage-profile/security/api-tokens";

    // Exact prompt locked by CONTEXT D-UI-03 (and mirrored in the docs per D-DOC-03/D-DOC-02 §6).
    // Any edit here MUST also update docs/docs/jira-widget.mdx to keep the two copies identical.
    export const CLAUDE_SETUP_PROMPT =
        "~/.config/waveterm/jira.json 파일을 만들어줘. Atlassian site URL, cloudId, email, PAT(api token), JQL(assignee = currentUser() ORDER BY updated DESC), pageSize(50)을 물어봐서 채워줘. 파일은 권한 0600으로 저장.";

    export function classifyErrorMessage(msg: string | null | undefined): ErrorState | null {
        if (!msg) return null;
        // D-STATE-01 setup: missing config OR empty cache (D-UI-04 variant)
        if (msg.startsWith("설정 파일이 없습니다")) return "setup";
        if (msg.startsWith("캐시 파일이 비어있습니다")) return "setup";
        // D-STATE-02 auth
        if (msg.startsWith("인증 실패")) return "auth";
        // D-STATE-03 network (includes rate-limit per CONTEXT)
        if (msg.startsWith("Jira 서버에 연결할 수 없습니다")) return "network";
        if (msg.startsWith("Jira 서버가 요청을 제한했습니다")) return "network";
        // D-STATE-04 default
        return "unknown";
    }
    ```
    Run the test again — MUST pass.

    Specificity notes:
    - Use `.startsWith()` (not `===`) because network messages carry a sanitized tail and the auth message may evolve in wording after the em-dash.
    - Do NOT add a React import here — this file must be pure-TS so it tests without a DOM.
    - CLAUDE_SETUP_PROMPT is a single-line string (no leading/trailing whitespace) — matches D-UI-03 exactly.
  </action>
  <verify>
    <automated>cd F:/Waveterm/waveterm && npx vitest run frontend/app/view/jiratasks/jiratasks-errorstate.test.ts --reporter=verbose</automated>
  </verify>
  <done>jiratasks-errorstate.ts exports classifyErrorMessage / ErrorState / CLAUDE_SETUP_PROMPT / ATLASSIAN_PAT_URL; all test cases pass; no React dependency in the helper file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire 4-state error cards into widget + remove legacy banner</name>
  <files>frontend/app/view/jiratasks/jiratasks.tsx, frontend/app/view/jiratasks/jiratasks.scss</files>
  <behavior>
    Rendering expectations (verified via TS type-check + manual DOM inspection; no RTL harness):
    - `classifyErrorMessage(error)` is called once per render inside the JSX return of the functional component at jiratasks.tsx (~line 976).
    - When state is `"setup"`: card shows `<i className="fa-solid fa-gear" />`, title "Jira 설정 필요" OR "아직 새로고침하지 않았습니다" (empty-cache variant), body explains needs `~/.config/waveterm/jira.json`, 2 CTA buttons: "📋 Claude에게 자동 설정 요청" and "📖 README 보기".
    - When state is `"auth"`: card shows `fa-key` icon, title "인증 실패 — PAT 재발급 필요", body mentions apiToken invalid/expired, 2 CTAs: "🔑 Atlassian PAT 페이지 열기" (opens webview block) and "📝 jira.json 편집 안내" (opens README webview).
    - When state is `"network"`: card shows `fa-wifi` icon, title "네트워크 오류", CTA "🔄 다시 시도" wired to `model.requestJiraRefresh()`.
    - When state is `"unknown"`: card shows `fa-triangle-exclamation` icon, body shows `{error}` verbatim (already sanitized by backend), CTAs "🔄 다시 시도" and "📖 README".
    - "Claude에게 자동 설정 요청" click: calls `navigator.clipboard.writeText(CLAUDE_SETUP_PROMPT)` then sets a local toast atom/state to "클립보드에 복사됨 — Claude 터미널에 붙여넣기" for ~3s.
    - Empty-cache detection: when `error` matches `classifyErrorMessage` → "setup" AND the source was "캐시 파일이 비어있습니다...", render the empty-cache copy variant per D-UI-04.
    - Legacy banner (jiratasks.tsx:1082-1087, `<div className="jiratasks-error">`) is DELETED, not left alongside the new cards (D-REG-01).
  </behavior>
  <action>
    Sub-steps:

    **(a) Import helper at top of jiratasks.tsx:**
    ```ts
    import {
        classifyErrorMessage,
        CLAUDE_SETUP_PROMPT,
        ATLASSIAN_PAT_URL,
        type ErrorState,
    } from "./jiratasks-errorstate";
    ```

    **(b) Add clipboard-toast state inside the `JiraTasksView` component (~line 914):**
    ```ts
    const [copyToast, setCopyToast] = React.useState<string | null>(null);
    ```
    (If React default import not present, use `useState` — already available via existing `useCallback, useEffect, useRef` import on line 13. Add `useState` to that import.)

    **(c) Add handlers just before the `return (` on line 976:**
    ```ts
    const handleCopyClaudePrompt = useCallback(() => {
        fireAndForget(async () => {
            await navigator.clipboard.writeText(CLAUDE_SETUP_PROMPT);
            setCopyToast("클립보드에 복사됨 — Claude 터미널에 붙여넣기");
            setTimeout(() => setCopyToast(null), 3000);
        });
    }, []);
    const handleOpenPatPage = useCallback(() => {
        fireAndForget(() => createBlock({ meta: { view: "web", url: ATLASSIAN_PAT_URL } }));
    }, []);
    const handleOpenReadme = useCallback(() => {
        // Internal docs route served by Docusaurus — page created in Plan 04-02.
        fireAndForget(() => createBlock({ meta: { view: "web", url: "https://docs.waveterm.dev/jira-widget" } }));
    }, []);
    const handleRetry = useCallback(() => {
        fireAndForget(() => model.requestJiraRefresh());
    }, [model]);
    ```
    Note: if `requestJiraRefresh` doesn't exist on the model yet (Phase 3 should have added it — verify via grep), fall back to `model.loadFromCache()` for network/unknown retry; prefer `requestJiraRefresh` when present.

    **(d) Compute errorState + empty-cache flag inside the render body:**
    ```ts
    const errorState = classifyErrorMessage(error);
    const isEmptyCache = error?.startsWith("캐시 파일이 비어있습니다") ?? false;
    ```

    **(e) REPLACE the legacy block at jiratasks.tsx:1082-1087:**

    DELETE:
    ```tsx
    {error && (
        <div className="jiratasks-error">
            <i className="fa-solid fa-triangle-exclamation" />
            <span>{error}</span>
        </div>
    )}
    ```

    INSERT a switch-style render. Keep it inline (no new component file — Claude's discretion per CONTEXT):
    ```tsx
    {errorState === "setup" && (
        <div className="jiratasks-state-card state-setup">
            <div className="state-icon"><i className="fa-solid fa-gear" /></div>
            <div className="state-title">{isEmptyCache ? "아직 새로고침하지 않았습니다" : "Jira 설정 필요"}</div>
            <div className="state-body">
                {isEmptyCache
                    ? <>☁️ 버튼을 눌러 Jira에서 이슈를 가져오세요.</>
                    : <>
                        <code>~/.config/waveterm/jira.json</code> 파일이 필요합니다.
                        아래 버튼을 눌러 Claude에게 설정을 요청하거나 README를 확인하세요.
                    </>}
            </div>
            <div className="state-ctas">
                {isEmptyCache ? (
                    <button className="cta cta-primary" onClick={handleRetry}>
                        <i className="fa-solid fa-cloud-arrow-down" /> 새로고침
                    </button>
                ) : (
                    <>
                        <button className="cta cta-primary" onClick={handleCopyClaudePrompt}>
                            <i className="fa-solid fa-clipboard" /> Claude에게 자동 설정 요청
                        </button>
                        <button className="cta cta-secondary" onClick={handleOpenReadme}>
                            <i className="fa-solid fa-book" /> README 보기
                        </button>
                    </>
                )}
            </div>
            {copyToast && <div className="state-toast">{copyToast}</div>}
        </div>
    )}
    {errorState === "auth" && (
        <div className="jiratasks-state-card state-auth">
            <div className="state-icon"><i className="fa-solid fa-key" /></div>
            <div className="state-title">인증 실패 — PAT 재발급 필요</div>
            <div className="state-body">
                <code>apiToken</code>이 유효하지 않거나 만료되었습니다.
                Atlassian에서 새 API token을 발급받아 <code>~/.config/waveterm/jira.json</code>에 붙여넣으세요.
            </div>
            <div className="state-ctas">
                <button className="cta cta-primary" onClick={handleOpenPatPage}>
                    <i className="fa-solid fa-key" /> Atlassian PAT 페이지 열기
                </button>
                <button className="cta cta-secondary" onClick={handleOpenReadme}>
                    <i className="fa-solid fa-book" /> jira.json 편집 안내
                </button>
            </div>
        </div>
    )}
    {errorState === "network" && (
        <div className="jiratasks-state-card state-network">
            <div className="state-icon"><i className="fa-solid fa-wifi" /></div>
            <div className="state-title">네트워크 오류</div>
            <div className="state-body"><span className="error-raw">{error}</span></div>
            <div className="state-ctas">
                <button className="cta cta-primary" onClick={handleRetry}>
                    <i className="fa-solid fa-rotate-right" /> 다시 시도
                </button>
            </div>
        </div>
    )}
    {errorState === "unknown" && (
        <div className="jiratasks-state-card state-unknown">
            <div className="state-icon"><i className="fa-solid fa-triangle-exclamation" /></div>
            <div className="state-title">새로고침 실패</div>
            <div className="state-body"><span className="error-raw">{error}</span></div>
            <div className="state-ctas">
                <button className="cta cta-primary" onClick={handleRetry}>
                    <i className="fa-solid fa-rotate-right" /> 다시 시도
                </button>
                <button className="cta cta-secondary" onClick={handleOpenReadme}>
                    <i className="fa-solid fa-book" /> README
                </button>
            </div>
        </div>
    )}
    ```

    **(f) Add SCSS for the new card classes (append to jiratasks.scss, near the existing `.jiratasks-error` block at line 381):**
    ```scss
    .jiratasks-state-card {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 10px;
        margin: 24px auto;
        padding: 28px 24px;
        max-width: 480px;
        border-radius: 8px;
        border: 1px solid var(--border-color);
        background: var(--panel-bg-color, rgba(255, 255, 255, 0.03));
        text-align: center;

        .state-icon {
            font-size: 32px;
            color: var(--secondary-text-color);
            opacity: 0.8;
        }
        .state-title {
            font-size: 15px;
            font-weight: 600;
            color: var(--main-text-color);
        }
        .state-body {
            font-size: 13px;
            color: var(--secondary-text-color);
            line-height: 1.5;
            code {
                background: rgba(0, 0, 0, 0.2);
                padding: 1px 6px;
                border-radius: 3px;
                font-family: var(--termfontfamily, monospace);
                font-size: 12px;
            }
            .error-raw {
                display: block;
                margin-top: 6px;
                font-family: var(--termfontfamily, monospace);
                font-size: 12px;
                color: var(--error-color, #f87171);
                word-break: break-word;
            }
        }
        .state-ctas {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            justify-content: center;
            margin-top: 4px;
        }
        .cta {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 12px;
            border-radius: 4px;
            font-size: 12px;
            cursor: pointer;
            border: 1px solid var(--border-color);
            background: transparent;
            color: var(--main-text-color);
            transition: background 0.15s, border-color 0.15s;
            &:hover { border-color: #a855f7; }
        }
        .cta-primary {
            background: rgba(168, 85, 247, 0.15); // purple accent per existing "댓글" pattern
            border-color: rgba(168, 85, 247, 0.5);
            color: #c4b5fd;
            &:hover { background: rgba(168, 85, 247, 0.28); }
        }
        .state-toast {
            margin-top: 6px;
            padding: 4px 10px;
            font-size: 12px;
            color: #a7f3d0;
            background: rgba(16, 185, 129, 0.12);
            border-radius: 4px;
        }

        &.state-setup  .state-icon { color: #a855f7; }
        &.state-auth   .state-icon { color: #f59e0b; }
        &.state-network .state-icon { color: #3b82f6; }
        &.state-unknown .state-icon { color: #f87171; }
    }
    ```

    **(g) Tighten loading-gate logic:** the existing loading spinner should not overlap with the state card. Update the guard so the card only renders when `!loading`:
    ```tsx
    {!loading && errorState === "setup" && (...)}
    {!loading && errorState === "auth"  && (...)}
    // etc.
    ```

    **(h) Keep `{refreshProgress && ...}` block as-is** (it's success-path summary, not an error).

    **Do NOT** introduce react-testing-library — not in devDependencies and adding it would blow scope. Render correctness is verified by TS type-check + the existing vitest unit tests on `classifyErrorMessage`. Visual UAT is deferred to Phase 4 HUMAN-UAT.md per D-TEST-02.

    Per D-REG-02: do NOT touch `loadFromCache`'s silent-failure branch (lines 484-529). The error strings it sets ARE the contract inputs.

    Per CONTEXT traceability: every JSX branch above exists because of D-STATE-01 (setup), D-STATE-02 (auth), D-STATE-03 (network), D-STATE-04 (unknown), D-UI-02 (switch render), D-UI-03 (clipboard prompt), D-UI-04 (empty-cache variant), D-REG-01 (legacy banner removal).
  </action>
  <verify>
    <automated>cd F:/Waveterm/waveterm && npx tsc --noEmit -p tsconfig.json 2>&1 | tee /tmp/jiratasks-tsc.log; grep -E "jiratasks\.(tsx|scss)|jiratasks-errorstate" /tmp/jiratasks-tsc.log | grep -i error; test $? -ne 0</automated>
  </verify>
  <done>
    - TypeScript compiles with no errors pointing at jiratasks.tsx, jiratasks.scss, or jiratasks-errorstate.ts.
    - `grep -n "jiratasks-error" frontend/app/view/jiratasks/jiratasks.tsx` returns no matches in the JSX render block (legacy banner removed; the SCSS class can remain unused temporarily but PREFER removing it to keep repo clean).
    - `grep -n "classifyErrorMessage\|CLAUDE_SETUP_PROMPT\|ATLASSIAN_PAT_URL" frontend/app/view/jiratasks/jiratasks.tsx` returns matches confirming wire-up.
    - Vitest helper tests from Task 1 still pass.
    - Visual UAT deferred to Phase 4 HUMAN-UAT.md (D-TEST-02) — record scenarios there: (1) delete ~/.config/waveterm/jira.json → setup card, (2) corrupt apiToken → auth card, (3) offline → network card, (4) empty cache → setup+empty variant, (5) clipboard toast fires.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run frontend/app/view/jiratasks/jiratasks-errorstate.test.ts` — all cases GREEN
- `npx tsc --noEmit` — no new TS errors in `frontend/app/view/jiratasks/`
- `grep -n 'classifyErrorMessage' frontend/app/view/jiratasks/jiratasks.tsx` — at least 1 call site
- `grep -n 'className="jiratasks-error"' frontend/app/view/jiratasks/jiratasks.tsx` — 0 matches (legacy banner removed per D-REG-01)
- `grep -n 'CLAUDE_SETUP_PROMPT\|ATLASSIAN_PAT_URL' frontend/app/view/jiratasks/jiratasks-errorstate.ts` — present
- Manual UAT items recorded in Phase 4 HUMAN-UAT.md (produced at phase end per D-TEST-02)
</verification>

<success_criteria>
- JIRA-04 satisfied: missing-config, invalid-token, network, unknown states are visually distinct and each carries a fix action.
- Pure helper (`classifyErrorMessage`) is independently testable (Nyquist RED→GREEN cycle committed).
- Legacy single-line banner replaced (D-REG-01); no duplicated intent.
- Every CONTEXT decision D-STATE-01..04, D-UI-01..04, D-REG-01, D-TEST-01 is addressed by code in this plan (D-DOC-01..04 live in Plan 04-02; D-TEST-02 UAT doc produced at phase end).
</success_criteria>

<output>
After completion, create `.planning/phases/04-setup-ux-docs/04-01-SUMMARY.md`.
</output>
